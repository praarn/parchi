name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: backend · ruff + pytest
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: bureaucracy
          POSTGRES_PASSWORD: bureaucracy
          POSTGRES_DB: bureaucracy
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U bureaucracy -d bureaucracy"
          --health-interval 5s --health-timeout 5s --health-retries 10
    env:
      DATABASE_URL: postgresql://bureaucracy:bureaucracy@localhost:5432/bureaucracy
      JWT_SECRET: ci-secret-not-for-production-0123456789abcdef
      GROQ_API_KEY: ci-dummy-key
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt
      - run: pip install -r requirements-dev.txt
      - name: Apply schema
        run: psql "$DATABASE_URL" -f ../database/schema.sql
      - run: ruff check .
      - run: ruff format --check .
      - run: pytest -q

  frontend:
    name: frontend · lint + typecheck + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build

  docker:
    name: docker · compose build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: cp backend/.env.example backend/.env
      - run: docker compose build

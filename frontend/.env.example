# Base URL of the Parchi FastAPI service.
NEXT_PUBLIC_API_URL=http://localhost:8000
# WebSocket origin for live processing progress (usually the same host as the API).
NEXT_PUBLIC_WS_URL=ws://localhost:8000
